= form_for @interview,url: interview_index_path, local: true do |form|
  .title
    = form.label :name
    = form.text_field :name
  .time
    = form.label :start_time
    = form.datetime_select :start_time
  .submit
    = form.submit