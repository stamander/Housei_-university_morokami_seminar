= form_for @private,url: interview_index_path, local: true do |form|
  .title
    = form.label :title
    = form.text_field :title
  .time
    = form.label :start_time
    = form.datetime_select :start_time
  .submit
    = form.submit