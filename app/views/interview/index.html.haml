.blogs
  .title 面接時間
  %table
    = link_to "投稿する",new_interview_path 
    %thead

    %tbody
      - @interview.each do |blog|
        %tr
          %td= blog.name
          %td= blog.start_time.strftime("%Y-%m-%d %H:%M")
          %td= link_to 'Show', blog

  %br/
  = month_calendar events: @interview do |date, blogs|
    = date.day
    - blogs.each do |blog|
      %div
        = blog.name